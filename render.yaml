services:
  - type: web
    name: crack-detection-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn crack_detection_web.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
